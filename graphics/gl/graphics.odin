package graphics

// This method will alias the currently used api and building abstractions with the api

// This could later be switched with when statements or even a vtable
import api "gl"

clear :: api.clear;

Shader :: api.Shader;